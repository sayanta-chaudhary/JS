Class hoisting?

<!-- Objects vs Maps -->
<!-- Arrays vs Sets -->

- Getters/Setters

prototypes -> regular functions -> factory/constructor functions -> classes -> inheritance 💡
<!-- Constructor functions: Regular function used with `new` to create multiple similar objects (instances) -->
<!-- - Old classic way (before ES6) -->

Classes: Syntactic sugar over constructor functions
`this` = access current object's properties /methods
`super` = access parent class's properties / methods
<!-- calling the constructor of the inherited class -->
Static property/method = belongs to the class/constructor itself, not to its instances
Static = Class-level property/method (whether built-in or custom) static = exists once

Private & protected properties / methods?
Composition over inheritance?
