# Objects

What exactly is an object?
- Encapsulated unit of data & behaviour

https://javascript.info/object
- Square brackets: For multiword properties
- Computed property: https://ui.dev/computed-property-names
- "key" in obj: To check if a property with the given key exists
- Are objects ordered?
- for...in vs Object.keys?

[object Object] is the default string representation of a plain object
JavaScript cannot represent objects as text by default

# Arrays

Q: Why array exist?
A: To store ordered data collections

What is Array?
- Array indexes are object properties internally
  Objects = dictionary (key: value)
  Arrays = list (ordered collections)
